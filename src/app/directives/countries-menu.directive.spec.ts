import { CountriesMenuDirective } from './countries-menu.directive';

describe('CountriesMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new CountriesMenuDirective();
    expect(directive).toBeTruthy();
  });
});
